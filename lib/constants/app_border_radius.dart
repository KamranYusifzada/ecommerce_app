import 'package:flutter/material.dart';

class AppBorderRadius {
  AppBorderRadius._();

  static BorderRadius all50 = const BorderRadius.all(Radius.circular(50));
  static BorderRadius all16 = const BorderRadius.all(Radius.circular(16));
}
